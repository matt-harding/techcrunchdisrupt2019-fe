# Enron Email example

```
CALL algo.pageRank(
  'MATCH (a:Account) WHERE exists( (a)<-[:TO|CC|BCC]-(:Email) ) RETURN id(a) as id LIMIT',
  'MATCH (a1:Account)<-[:TO|CC|BCC]-(:Email)<-[:SENT]-(a2:Accout) RETURN id(a2) as source, id(a3) as target',
  {graph:'cypher', write: true}
)
```